## Логика определения этапов интервальной тренировки

Этот документ фиксирует логику, по которой из ряда пульса выделяются:
разминка, ускорения, восстановления и заминка.

### 1. Подготовка данных

- Исходные данные: ряд пульса `HR(t)` с шагом 1 секунда.
- Сглаживание пульса:
  - `HR_20(t)` — скользящее среднее по HR с окном **20 секунд**, центрированное.
- Производная:
  - `dHR_20(t) = HR_20(t) - HR_20(t - 1)`.
- Дополнительное сглаживание производной:
  - `dHR_20_40(t)` — скользящее среднее по `dHR_20` с окном **40 секунд**, центрированное.
- Все последующие решения принимаются по **`dHR_20_40(t)`**.
- При анализе **игнорируются первые 2 минуты** (`t < 120 c`), чтобы не ловить артефакты старта.

### 2. Поиск пиков производной (плато‑подход)

Ищем экстремумы `up` (начала ускорений) и `down` (начала восстановлений) по сглаженной производной `dHR_20_40(t)`.

#### 2.1. Up‑пики (рост)

1. Находим все непрерывные отрезки времени, где:
   - `dHR_20_40(t) >= +0.25`.
2. Для каждого такого отрезка `[t_start, t_end]`:
   - выбираем момент `t_max` с максимальным значением `dHR_20_40` внутри отрезка;
   - если максимум достигается на плато (несколько точек подряд с одинаковым значением), берём **первую по времени** точку.
3. Формируем список up‑пиков:
   - `up = { (t_up_i, value_up_i) }`.

#### 2.2. Down‑пики (падение)

1. Находим все непрерывные отрезки времени, где:
   - `dHR_20_40(t) <= -0.40`.
2. Для каждого такого отрезка `[t_start, t_end]`:
   - выбираем момент `t_min` с минимальным (наиболее отрицательным) значением `dHR_20_40`;
   - при плато берём первую по времени точку.
3. Формируем список down‑пиков:
   - `down = { (t_down_j, value_down_j) }`.

#### 2.3. Объединение и схлопывание однотипных пиков

1. Объединяем все экстремумы и сортируем по времени:
   - `extrema_raw = sorted( up ∪ down )` по `t`.
2. Схлопываем подряд идущие однотипные пики:
   - итерируем `extrema_raw` слева направо;
   - если текущий экстремум имеет тот же тип (`up` или `down`), что и предыдущий, **игнорируем** его;
   - в результате получаем список:
     - `extrema = [ (t₁, type₁, dHR₁), (t₂, type₂, dHR₂), ... ]`,
     - где типы строго чередуются: `up / down / up / down / ...`.

### 3. Разминка

- Начало разминки:
  - `t_warmup_start = 0`.
- Конец разминки:
  - `t_warmup_end = t_first_up`,
  - где `t_first_up` — время **первого** экстремума с `type == "up"` в списке `extrema`.
- Интервал разминки:
  - `Warmup = [ t_warmup_start, t_warmup_end )`.

Интуитивно: пока нет устойчивого положительного тренда `dHR` (первого большого `up`‑пика), считаем, что идёт разминка.

### 4. Интервалы ускорение/восстановление

Используем чередующийся список `extrema`:

- каждый `up` считается началом ускорения;
- каждый `down` считается началом восстановления.

Для последовательности:

```text
... up₁, down₁, up₂, down₂, ..., upₙ, downₙ
```

определяем:

- **ускорение K**:
  - `Acceleration_K = [ t_up_K, t_down_K )`;
- **восстановление K**:
  - `Recovery_K = [ t_down_K, t_up_(K+1) )`, для `K < N`;
- для последнего интервала:
  - `Acceleration_N = [ t_up_N, t_down_N )`;
  - `Recovery_N` идёт до начала заминки (см. ниже).

Таким образом, внутренние этапы тренировки выглядят как последовательность:

```text
Warmup -> Accel_1 -> Rec_1 -> Accel_2 -> Rec_2 -> ... -> Accel_N -> Rec_N -> Cooldown
```

### 5. Заминка

Есть два уровня строгости правила.

#### 5.1. Простое правило

- Берём последний down‑пик в списке `extrema`:
  - `t_last_down` — максимальное `t` среди экстремумов с `type == "down"`.
- Интервал заминки:
  - `Cooldown = [ t_last_down, t_end_training )`,
  - где `t_end_training` — конец записи тренировки.

#### 5.2. Уточнённое правило по падению амплитуды

Это правило учитывает, что после последнего “рабочего” интервала амплитуда `dHR` заметно падает.

1. Пусть `t_last_peak` — время последнего экстремума, считающегося частью интервалов  
   (обычно это тот же `t_last_down`, где `|dHR_last_peak|` достаточно велик).
2. Строим список **более мягких экстремумов** по тому же `dHR_20_40(t)` с порогом `0.2`:
   - up: отрезки, где `dHR_20_40(t) >= +0.2`;
   - down: отрезки, где `dHR_20_40(t) <= -0.2`;
   - внутри каждого отрезка снова берём максимум/минимум.
3. Сортируем такие экстремумы по времени и ищем **первый экстремум после `t_last_peak`**, для которого:

   \[
   |dHR\_{\text{last\_peak}} - dHR\_{\text{next}}| > 0.2
   \]

4. Время этого экстремума считаем началом заминки:
   - `t_cooldown_start = t_next`,
   - `Cooldown = [ t_cooldown_start, t_end_training )`.

Интуитивно: как только “качели” производной резко “сдуваются” (переход от большого по модулю пика к существенно меньшему), это сигнал к началу заминки.

### 6. Резюме по этапам

В итоге тренировку делим на последовательность отрезков по времени:

1. `Warmup = [0, t_first_up)`
2. Для каждого `K = 1..N`:
   - `Acceleration_K = [ t_up_K, t_down_K )`
   - `Recovery_K = [ t_down_K, t_up_(K+1) )` (для последнего интервала — до начала заминки)
3. `Cooldown = [ t_cooldown_start, t_end_training )`

Где:

- времена `t_up_K`, `t_down_K` и `t_cooldown_start` определяются по сглаженной производной `dHR_20_40(t)` описанными выше правилами (плато‑поиск, пороги `0.25` и `-0.40`, а для уточнения заминки — порог `0.2` по амплитуде).

